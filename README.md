# 📰 Испанские Кварталы - Telegram Mini App

Бот для сбора и модерации новостей района с веб-интерфейсом (Telegram Mini App).

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/EgorLesNet/ispanskie_msk_bot_ver)

## ✨ Возможности

### Для пользователей
- 📝 Отправка текстовых новостей в бот
- 📸 Прикрепление фото и видео
- 📱 Просмотр ленты новостей через Mini App
- 👍 Лайки и дизлайки для новостей
- 🏪 Просмотр местных бизнесов на карте
- ⭐ Отзывы и рейтинги бизнесов

### Для администратора
- ✅ Модерация новостей через кнопки в Telegram
- 🚀 Мгновенная публикация своих постов
- 🗑️ Удаление новостей командой `/delete`
- 📊 Управление базой данных через GitHub

## 🚀 Быстрый старт

### 1. Создание бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните полученный токен (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройка GitHub Token

**Это критически важно для работы бота!**

1. Перейдите в [GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)](https://github.com/settings/tokens)
2. Нажмите **Generate new token (classic)**
3. Дайте название: `ispanskie-bot`
4. Выберите срок действия: `No expiration` или на ваше усмотрение
5. **Обязательно** отметьте scope: `repo` (полный доступ к репозиториям)
6. Нажмите **Generate token**
7. **Скопируйте токен сразу** (он больше не отобразится!)

### 3. Деплой на Vercel

#### Через GitHub (рекомендуется)

1. Форкните этот репозиторий
2. Зайдите на [vercel.com](https://vercel.com)
3. Нажмите **New Project**
4. Импортируйте ваш форк
5. Добавьте Environment Variables:

```bash
BOT_TOKEN=ваш_токен_от_BotFather
GITHUB_TOKEN=ваш_github_token
WEBAPP_URL=https://ваш-проект.vercel.app
ADMIN_USERNAME=ваш_telegram_username
ADMIN_CHAT_ID=ваш_telegram_id
GITHUB_REPO=ваш_username/ispanskie_msk_bot_ver
DB_FILE_PATH=db.json
DB_BRANCH=main
BUSINESS_ADMIN_KEY=любой_секретный_ключ
```

6. Нажмите **Deploy**

### 4. Настройка Webhook

После успешного деплоя выполните команду (замените значения):

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_BOT_TOKEN>/setWebhook?url=https://ваш-проект.vercel.app/api/telegram"
```

Должен вернуться ответ:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

### 5. Получение ADMIN_CHAT_ID

1. Напишите боту `/start`
2. Проверьте логи в Vercel Dashboard → Functions → Logs
3. Найдите ваш chat ID в логах
4. Добавьте его в Environment Variables как `ADMIN_CHAT_ID`
5. Redeploy проекта

## 📁 Структура проекта

```
ispanskie_msk_bot_ver/
├── api/                      # Serverless функции (Vercel)
│   ├── _db.js               # Модуль работы с БД (GitHub API)
│   ├── telegram.js          # Webhook обработчик бота
│   ├── news.js              # API получения новостей
│   ├── media.js             # Проксирование медиа из Telegram
│   ├── businesses.js        # API бизнесов
│   └── reactions.js         # API лайков/дизлайков
│
├── public/                   # Статические файлы веб-приложения
│   ├── news.html            # Лента новостей
│   ├── business.html        # Карта бизнесов
│   ├── index.js             # Логика фронтенда
│   ├── styles.css           # Стили
│   └── db.json              # Начальная БД (копируется в /tmp)
│
├── db.json                   # База данных (управляется ботом)
├── index.js                  # Локальный сервер для разработки
├── package.json             # Зависимости
├── vercel.json              # Конфигурация Vercel
└── .env.example             # Пример переменных окружения
```

## 🤖 Использование бота

### Команды

- `/start` - Приветствие и кнопка открытия Mini App
- `/delete <id>` - Удалить новость по ID (только админ)
- `/delete` (reply) - Удалить новость ответом на сообщение модерации (только админ)

### Отправка новостей

**Для обычных пользователей:**
1. Отправьте текст или фото с подписью
2. Получите подтверждение: "📩 Новость отправлена на проверку"
3. Дождитесь одобрения админом

**Для админа:**
1. Отправьте текст или фото с подписью
2. Новость публикуется мгновенно: "✅ Новость опубликована!"

### Модерация (только админ)

1. Получите уведомление о новой новости
2. Нажмите кнопку:
   - **✅ Одобрить** - публикует новость
   - **❌ Отклонить** - отправляет в архив

## 🌐 Web API

### GET /api/news
Получить все одобренные новости

```javascript
fetch('https://ваш-проект.vercel.app/api/news')
  .then(r => r.json())
  .then(data => console.log(data.posts))
```

**Ответ:**
```json
{
  "posts": [
    {
      "id": 1,
      "text": "Текст новости",
      "authorName": "Имя автора",
      "createdAt": "2026-01-26T10:00:00.000Z",
      "media": [{"type": "photo", "fileId": "..."}],
      "likes": 5,
      "dislikes": 0
    }
  ],
  "total": 1,
  "timestamp": "2026-01-26T10:00:00.000Z"
}
```

### GET /api/media?fileId=...
Получить медиа файл из Telegram

```html
<img src="https://ваш-проект.vercel.app/api/media?fileId=AgACAgIAAxkBAAI..." />
```

### POST /api/reactions
Поставить лайк или дизлайк

```javascript
fetch('https://ваш-проект.vercel.app/api/reactions', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({postId: 1, type: 'like'})
})
```

### GET /api/businesses
Получить список бизнесов

## 🛠️ Локальная разработка

```bash
# Клонирование
git clone https://github.com/EgorLesNet/ispanskie_msk_bot_ver.git
cd ispanskie_msk_bot_ver

# Установка зависимостей
npm install

# Настройка окружения
cp .env.example .env
# Заполните .env своими значениями

# Запуск локального сервера
npm start

# Сервер запустится на http://localhost:3000
```

### Тестирование webhook локально

Используйте [ngrok](https://ngrok.com/) для туннелирования:

```bash
# В одном терминале
npm start

# В другом терминале
ngrok http 3000

# Скопируйте HTTPS URL и установите webhook:
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://xxxx.ngrok.io/api/telegram"
```

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что webhook установлен:
   ```bash
   curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
   ```
3. Проверьте Environment Variables в Vercel

### Новости не сохраняются

- **Причина:** Нет GITHUB_TOKEN или неправильные права
- **Решение:** 
  1. Убедитесь, что GITHUB_TOKEN добавлен в Vercel Environment Variables
  2. Проверьте, что токен имеет scope `repo`
  3. Проверьте GITHUB_REPO (формат: `username/repo`)

### Медиа не загружается

- Проверьте размер файла (лимит 20MB для Telegram Bot API)
- Проверьте, что BOT_TOKEN правильный
- Проверьте логи `/api/media` в Vercel

### 413 Payload Too Large

- Telegram Bot API поддерживает файлы только до 20MB
- Попросите пользователей отправлять файлы меньшего размера
- Или используйте сжатие изображений

## 📊 База данных

Проект использует `db.json` в корне репозитория как базу данных.

**Структура:**
```json
{
  "posts": [],        // Одобренные новости
  "pending": [],      // На модерации
  "rejected": [],     // Отклоненные
  "businesses": []    // Бизнесы
}
```

**Как работает:**
1. Бот пишет в `db.json` через GitHub API
2. Используется оптимистическая блокировка (retry при конфликте)
3. Кэширование на 5 секунд для снижения нагрузки

## 🔐 Безопасность

- Все секретные ключи хранятся в Environment Variables
- GITHUB_TOKEN не должен быть в коде
- Модерация через проверку `ADMIN_USERNAME` или `ADMIN_CHAT_ID`
- CORS настроен для безопасного доступа к API

## 📝 Лицензия

Unlicense - свободное использование

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте [Issues](https://github.com/EgorLesNet/ispanskie_msk_bot_ver/issues)
2. Создайте новый Issue с описанием проблемы
3. Приложите логи из Vercel Dashboard

---

**Сделано с ❤️ для района Испанские Кварталы**
